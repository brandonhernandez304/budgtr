<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/normalize.css" />
    <link rel="stylesheet" href="/skeleton.css" />
    <title>Budgtr App</title>
    <style>
        body{
            background-color: burlywood;
            color: darkolivegreen ;
        }
    </style>
</head>

<nav>
    <a href="/budget/new">Create a New Budget</a>
</nav>

<body>
    <h1>Budget Index Page</h1>
    <% for (i = 0; i < allBudget.length; i++) { %>
        <% bankAcc = bankAcc + parseInt(allBudget[i].amount) %>
        <% } %>
        <h2>Balance: $<%= bankAcc %></h2>
    <table>
        <thead>
            <tr>
                <th colspan="3">Budgtr</th>
            </tr>
        </thead>
        <tbody>
            <tr>

                <th scope="row">Date</th>
                <th scope="row">Name</th>
                <th scope="row">Amount</th>
            </tr>

            <% for(let i=0; i < allBudget.length; i++){ %>
                <tr>
                    <td>
                        <%=allBudget[i].date %>
                    </td>

                    <td>
                        <a href='/budget/<%=i%>'>
                            <%=allBudget[i].name %>
                        </a>
                    </td>
                    <td>
                        <%=allBudget[i].amount %>
                    </td>

                </tr>
                <a href="/budget/<%=i%>"></a>

                <% } %>

                    </tr>
        </tbody>
    </table>
</body>
<!-- time to make bankAcc -->
<% if(bankAcc < 0) {%>
    <style>
        body{
            background-color: darkred;
            color: white;
        }
    </style>
<% } %>
<% if (bankAcc > 1000){ %>
<style>
    body{
        background-color: lightblue;
        color: g;
    }
</style>
<% } %>

</html>